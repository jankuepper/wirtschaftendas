version: '3.7'
services:
  frontend:
    image: node:16
    volumes:
    - ./frontend:/app
    working_dir: /app
    command: bash -c "npm install && npm start"
    ports: 
      - 3000:3000
networks:
  front:
    driver: bridge